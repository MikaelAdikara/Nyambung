# Status jalur 4 — Dasbor
Diperbarui: 10:20

## Sedang dikerjakan
4.2 Lapisan data dua sumber (api | demo) + aggregate.ts cermin kontrak §5

## Tonggak selesai (tag)
- (belum ada tag) 4.1 kerangka di branch `jalur-4-dashboard`: Vite + React + TS tanpa pustaka UI/router/grafik,
  `npm run build` + `npm run lint` bersih, hash routing D1–D4 dicek di browser,
  `prepare-public` menyalin seed/demo_events.json, 85 png + custom/, core_vocab_id.csv ke public/

## Perkiraan tonggak berikutnya
j4-data sekitar 11:30

## Terblokir oleh
- (kosong)

## Permintaan ke jalur lain
- ke jalur 3: dev `http://127.0.0.1:5173`, preview `http://127.0.0.1:4173` (untuk README). Jalankan `npm install` lalu
  `npm run dev` di `dashboard/`; `predev`/`prebuild` otomatis menyalin aset ke `public/`.

## Perubahan API/kontrak yang perlu diketahui
- (kosong)

## Perubahan terhadap proposal (untuk PERUBAHAN.md, dibaca jalur 4)
- (kosong)
